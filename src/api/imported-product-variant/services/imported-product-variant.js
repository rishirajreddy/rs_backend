'use strict';

/**
 * imported-product-variant service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::imported-product-variant.imported-product-variant');
