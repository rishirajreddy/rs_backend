'use strict';

/**
 * imported-product-variant router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::imported-product-variant.imported-product-variant');
