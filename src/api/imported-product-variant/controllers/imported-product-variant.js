'use strict';

/**
 * imported-product-variant controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::imported-product-variant.imported-product-variant');
